# Default Configuration (Skeleton)
# Environment-specific overrides should be in apphosting.prod.yaml or apphosting.staging.yaml
env:
  # This secret mapping works for both envs if the secret name in Secret Manager is consistent
  - variable: GEMINI_API_KEY
    secret: GEMINI_API_KEY