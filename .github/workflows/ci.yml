name: CI

# No path filters on any trigger: fast-checks/integration/e2e are required status
# checks on main, so they must always report. Docs-only changes short-circuit via
# the `changes` job below — every expensive step is gated on `code == 'true'`, so
# the jobs still run and pass quickly instead of never reporting.
on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]
  # Required so CI runs on the GitHub merge queue's temporary ref. Without this the
  # required checks (fast-checks/integration/e2e) never report on queue entries and PRs
  # hang in the queue forever.
  merge_group:

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      # 'true' if anything outside the docs-ish paths changed. Non-PR events
      # (push, merge_group) always run the full pipeline.
      code: ${{ github.event_name != 'pull_request' && 'true' || steps.filter.outputs.code }}
      # The agent tooling under .github/scripts is excluded from `code` above,
      # so it needs its own signal or its tests would never run on a PR that
      # only touches it.
      agent_scripts: ${{ github.event_name != 'pull_request' && 'true' || steps.filter.outputs.agent_scripts }}
    steps:
      - name: Checkout
        if: github.event_name == 'pull_request'
        uses: actions/checkout@v4

      - name: Detect code changes
        if: github.event_name == 'pull_request'
        id: filter
        uses: dorny/paths-filter@v3
        with:
          predicate-quantifier: every
          filters: |
            code:
              - '!.github/**'
              - '!recipe-lanes/.husky/**'
              - '!docs/**'
              - '!recipe-lanes/scripts/**'
              - '!README.md'
              - '!LICENSE'
              - '!CONTRIBUTING.md'
            agent_scripts:
              - '.github/scripts/**'

  # Zero-dependency tests for the agent tooling (issue-claim protocol). Kept out
  # of fast-checks because that job is gated on `code` — which deliberately
  # excludes .github/** — and because this needs no npm install.
  agent-scripts:
    runs-on: ubuntu-latest
    needs: changes

    steps:
      - name: Skip notice (no agent script changes)
        if: needs.changes.outputs.agent_scripts != 'true'
        run: echo "No .github/scripts changes; skipping agent script tests."

      - name: Checkout
        if: needs.changes.outputs.agent_scripts == 'true'
        uses: actions/checkout@v4

      - name: Set up Node
        if: needs.changes.outputs.agent_scripts == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Test agent scripts
        if: needs.changes.outputs.agent_scripts == 'true'
        run: node --test .github/scripts/*.test.mjs

  fast-checks:
    runs-on: ubuntu-latest
    needs: changes

    steps:
      - name: Skip notice (docs-only change)
        if: needs.changes.outputs.code != 'true'
        run: echo "No code paths changed; skipping fast-checks work."

      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@v4

      - name: Set up Node
        if: needs.changes.outputs.code == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            recipe-lanes/package-lock.json

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm ci

      - name: Lint
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm run lint

      - name: Typecheck
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm run typecheck

      - name: Unit tests (pure)
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm run test:unit:pure

  integration:
    runs-on: ubuntu-latest
    needs: [changes, fast-checks]

    steps:
      - name: Skip notice (docs-only change)
        if: needs.changes.outputs.code != 'true'
        run: echo "No code paths changed; skipping integration work."

      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@v4

      - name: Set up Node
        if: needs.changes.outputs.code == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            recipe-lanes/package-lock.json
            recipe-lanes/functions/package-lock.json

      - name: Set up Java
        if: needs.changes.outputs.code == 'true'
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm ci

      - name: Install Firebase Tools
        if: needs.changes.outputs.code == 'true'
        run: |
          npm install -g firebase-tools
          firebase experiments:enable webframeworks

      # test-unit-integration.sh downloads the pinned Xenova/all-MiniLM-L6-v2
      # embedding model from huggingface.co whenever model-cache is absent —
      # on a fresh runner that was EVERY run, and one connection reset failed
      # the whole job. Cache it like the Playwright browsers in the e2e job;
      # bump the key if functions/scripts/download-model.js pins a new model.
      - name: Cache functions embedding model
        if: needs.changes.outputs.code == 'true'
        uses: actions/cache@v4
        with:
          path: recipe-lanes/functions/src/vector-search/model-cache
          key: hf-model-Xenova-all-MiniLM-L6-v2-fp32-v1

      - name: Build
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm run build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: "demo-key"
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: "local-project-id.firebaseapp.com"
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: "local-project-id"
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: "local-project-id.firebasestorage.app"
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "123456789"
          NEXT_PUBLIC_FIREBASE_APP_ID: "1:123456789:web:abcdef"

      - name: Integration tests (emulator-dependent)
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm run test:unit:integration
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: "demo-key"
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: "local-project-id"
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: "local-project-id.firebasestorage.app"
          NEXT_PUBLIC_USE_FIREBASE_EMULATOR: "true"
          GEMINI_API_KEY: "mock-key"

  e2e:
    runs-on: ubuntu-latest
    needs: [changes, fast-checks]

    steps:
      - name: Skip notice (docs-only change)
        if: needs.changes.outputs.code != 'true'
        run: echo "No code paths changed; skipping e2e work."

      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@v4

      - name: Set up Node
        if: needs.changes.outputs.code == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            recipe-lanes/package-lock.json

      - name: Set up Java
        if: needs.changes.outputs.code == 'true'
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm ci

      - name: Install Firebase Tools
        if: needs.changes.outputs.code == 'true'
        run: |
          npm install -g firebase-tools
          firebase experiments:enable webframeworks

      - name: Cache Playwright browsers
        if: needs.changes.outputs.code == 'true'
        id: cache-playwright
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-1.58.2

      - name: Install Playwright browsers
        if: needs.changes.outputs.code == 'true' && steps.cache-playwright.outputs.cache-hit != 'true'
        run: cd recipe-lanes && npx playwright install --with-deps

      - name: Install Playwright system deps (cached hit)
        if: needs.changes.outputs.code == 'true' && steps.cache-playwright.outputs.cache-hit == 'true'
        run: cd recipe-lanes && npx playwright install-deps

      - name: Build
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm run build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: "demo-key"
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: "local-project-id.firebaseapp.com"
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: "local-project-id"
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: "local-project-id.firebasestorage.app"
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "123456789"
          NEXT_PUBLIC_FIREBASE_APP_ID: "1:123456789:web:abcdef"

      - name: E2E tests
        if: needs.changes.outputs.code == 'true'
        run: cd recipe-lanes && npm run test:e2e
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: "demo-key"
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: "local-project-id"
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: "local-project-id.firebasestorage.app"
          NEXT_PUBLIC_USE_FIREBASE_EMULATOR: "true"
          GEMINI_API_KEY: "mock-key"
